machine:
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-19,build-tools-19.0.0"

  override:
    - gradle dependencies

test:
  override:
    - gradle build